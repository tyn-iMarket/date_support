require 'spec_helper'

describe DateSupport do
  it 'has a version number' do
    expect(DateSupport::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
